#ifndef _DETECT_H_
#define _DETECT_H_

namespace ttrk{

/**
 * @class Detect
 * @brief Detection system. Wraps the classification of images and interface with the TTrack
 * class.
 *
 * This is the class...
 */


  class Detect{
    
  public:

    Detect();
    ~Detect();
    
    
  };

}

#endif
